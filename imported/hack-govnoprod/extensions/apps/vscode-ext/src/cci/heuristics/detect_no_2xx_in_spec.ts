import { Finding } from '../types';

export const id = 'no_2xx_in_spec';

// Minimal stub — requires OpenAPI parsing; return empty for now
export function run(content: string, relpath: string): Finding[] {
  return [];
}


