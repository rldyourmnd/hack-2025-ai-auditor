import { Finding } from '../types';

export const id = 'schema_field_mismatch';

// Stub: requires parsing OpenAPI and Pydantic models; return empty for now
export function run(content: string, relpath: string): Finding[] { return []; }


