import { Finding } from '../types';

export const id = 'dependency_major_conflict';

// Stub — needs parsing of dependency files; return empty
export function run(content: string, relpath: string): Finding[] { return []; }


