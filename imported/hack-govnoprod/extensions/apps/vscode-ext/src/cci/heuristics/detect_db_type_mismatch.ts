import { Finding } from '../types';

export const id = 'db_type_mismatch';

// Stub: requires parsing SQL DDL vs ORM definitions — return empty
export function run(content: string, relpath: string): Finding[] { return []; }


