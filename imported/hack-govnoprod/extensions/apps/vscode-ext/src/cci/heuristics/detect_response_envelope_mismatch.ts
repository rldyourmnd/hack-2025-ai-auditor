import { Finding } from '../types';

export const id = 'response_envelope_mismatch';

// Stub — requires project-specific standard; return empty for now
export function run(content: string, relpath: string): Finding[] { return []; }


